services:
  - type: web
    name: mevtencia-app
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
    rootDirectory: backend
