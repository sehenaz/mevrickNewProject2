version: 1
services:
  - type: web
    name: mevtencia-app
    env: node
    plan: free
    rootDirectory: backend
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
