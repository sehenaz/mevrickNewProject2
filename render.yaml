version: 1
services:
  - type: web
    name: mevtencia-app
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
    rootDirectory: backend
